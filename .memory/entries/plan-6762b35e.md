---
id: plan-6762b35e
type: plan
project: semantopology_hardfork
parent_id: expe-fc0f2350
title: Measurement plumbing + compliance baseline before further CLIP layout work
node_label: 'Measurement plumbing + compliance baseline before '
tags: clip,measurement,plumbing,baseline,coordinator,sds,artifacts
status: active
open_threads: 0
success: 'null'
files: script/clip_saliency_compliance.py@da3a33c, script/clip_dream_layout.py@97ecab1,
  neural_structural_optimization/models/loss_semantic_prior.py@da3a33c
session_id: sess-90ddad13
created_at: '2026-09-08T13:09:15.318796+00:00'
updated_at: '2026-09-08T13:09:15.318796+00:00'
---
Approved coordinator run (rigor medium, all agents on cursor-grok-4.6-high).

WHY: audit found that no run saves its density as an array. Every result on disk is a min-max normalised 8-bit PNG, so re-scoring or adding a metric costs a full re-run. This is the tax behind every expensive question so far.

DO BEFORE NEXT RUN (these gate the science):
1. Save physical density as .npy in BOTH clip_saliency_compliance.run_arm and clip_dream_layout's physics stage.
2. _save_field min-max normalises every panel independently, so contrast depends on the field's extremes and two runs are not visually comparable. The gray-ink signature is masked or exaggerated depending on outliers. Add an absolute [0,1] mode for density; keep normalised for sensitivity/preference where scale is meaningless.
3. The two pipelines report disjoint metric sets. clip_dream_layout physics has spatial_mass_loss and mass_on_scaffold but no clip_loss_raw and no connectivity validity; clip_saliency_compliance has the reverse. Unify so results land on one table.

WHENEVER (zero science impact, re-ranked down from an earlier list):
4. Remove the 10 clip_saliency_compliance_* symlinks at the results root.
5. Collapse the duplicated 128x256 VeniceGoldenConfig literal (projected_full vs full_replay). Verified character-identical today, so this is a drift hazard, not a live bug.
6. Dedupe the triplicated _load_golden importlib block across three scripts.
7. Fold the hardcoded venation arms into --prompt.

THEN: run compliance-only at 128x256 and save the .npy. One artifact closes three holes at once: a proper tautology-gate baseline for this exact problem (the current default proxy is a three-bay frame from a different experiment and its own help text disclaims it), the missing CLIP discrimination floor (human_skull_compliance has clip_loss_raw 0.0 because CLIP was never built for that arm), and a fair visual reference.

RISK: the environment rule records 128x256 segfaulting in 4 of 5 runs when the OpenMP pin was absent, and notes a green run proves nothing because it is intermittent. A bare SIGSEGV with no traceback is that known failure, not new code.

SDS BANKED: FrozenDenoiser is a randomly-initialised, never-trained 3-layer conv net; its docstring says it exists to exercise the SDS interface without downloading Stable Diffusion. The prompt reaches it only as a 512-dim CLIP embedding collapsed by a random Linear(512,1) to ONE scalar broadcast uniformly. It is a random-prior control, not a diffusion prior. Keep the code, drop it from default arms, relabel in the README.

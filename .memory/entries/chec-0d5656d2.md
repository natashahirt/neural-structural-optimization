---
id: chec-0d5656d2
type: checkpoint
project: semantopology_hardfork
parent_id: plan-8d7e2397
title: 'Stage 0 complete: test suite went from 0 runnable to 13 passing; CHOLMOD enabled'
node_label: 'Stage 0 complete: test suite went from 0 runnable '
tags: stage-0,cleanup,test-suite,cholmod
status: active
open_threads: 0
success: 'null'
files: neural_structural_optimization/tests/__init__.py@724f371, neural_structural_optimization/tests/test_autograd.py@724f371,
  neural_structural_optimization/tests/test_physics.py@724f371, neural_structural_optimization/train/__init__.py@724f371,
  .gitignore@724f371
session_id: sess-88c4b9c3
created_at: '2026-09-03T22:09:06.212595+00:00'
updated_at: '2026-09-03T22:09:06.212595+00:00'
results: '[{"metric": "tests_passing", "value": 13, "unit": "count", "split": "neural_structural_optimization/tests",
  "criterion": "suite imports and runs", "source": "stage-0"}, {"metric": "tests_passing_before",
  "value": 0, "unit": "count", "split": "neural_structural_optimization/tests", "criterion":
  "baseline before stage 0; collection aborted", "source": "stage-0"}, {"metric":
  "suite_runtime", "value": 10.2, "unit": "s", "split": "neural_structural_optimization/tests",
  "criterion": "with CHOLMOD", "source": "stage-0"}, {"metric": "suite_runtime_superlu",
  "value": 51.3, "unit": "s", "split": "neural_structural_optimization/tests", "criterion":
  "SuperLU fallback, before scikit-sparse", "source": "stage-0"}]'
---
Rebased local dev onto origin/dev. The two local-only commits were pure .memory/ bookkeeping, so the rebase replayed cleanly with no source conflicts.

FOUR DEFECTS FOUND, none of which were the one predicted.

1. .gitignore was mixed-encoding: 28 bytes of UTF-8 followed by UTF-16LE. Git could not parse anything past '*.png', which silently disabled *.nc, script/test_results_pytorch/, and the entire angelo block (.angelo/, .memory/active-sessions/, .claude/settings.local.json). This explains commit 8e4678b, 'Remove lingering .nc blobs and ignore them', whose ignore never took effect. Note angelo doctor reports '.gitignore has the angelo block' because the text is present, even though git cannot read it. Rewritten as UTF-8 and merged with origin/dev's entries (.venv/, logs/). Committed separately as 247e1ed.

2. THE SUITE'S ACTUAL BLOCKER was tests/__init__.py eagerly importing every test module. test_pipeline's missing apache_beam therefore aborted collection for test_autograd and test_physics too, so ZERO tests ran. Made __init__.py inert.

3. test_autograd.py did 'import autograd.numpy' and then 'from neural_structural_optimization.structural import autograd', shadowing the third-party package with this repo's own module of the same name. test_find_root_grad had therefore NEVER executed. Aliased to topo_autograd; the test now runs and passes. This matters beyond hygiene: it independently verifies the reverse-mode gradient of find_root, which stage 1's post-projection volume enforcement depends on.

4. test_physics.py::test_run_toposim asserted 'l > 0' against run_toposim's loss LIST, and set args['maxloop'] while the loop reads args['opt_steps'].

Also: test_pipeline.py imports neural_structural_optimization.pipeline, which no longer exists after the restructure. Dead like test_structural_parity.py; both deleted. Fixed the six phantom names in train/__init__.py's __all__ that broke star-import. Untracked 28 committed .pyc files.

CHOLMOD: scikit-sparse was not installed, so physics ran on the SuperLU fallback that the code itself warns is 'about twice as slow'. Installed into the neural_structural_opt conda env; HAS_CHOLMOD is now True.

GOTCHA discovered by the switch: test_run_toposim then failed on 'assert 0.4000000000000018 <= 0.4'. The volume constraint holds to ~1e-15; CHOLMOD and SuperLU simply differ in the last ulp and the test used an exact <=. Added a 1e-9 tolerance. This also empirically confirms the cone filter preserves the mean, and gives the tolerance to use for stage 1's volume assertions.

Environment: neural_structural_opt is torch 2.2.2, numpy 1.26.4, scipy 1.16.0. The numpy <2.0 ceiling is required by HIPS autograd.
